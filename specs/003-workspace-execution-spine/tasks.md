# Tasks: Workspace Execution Spine

This checklist records implementation/evidence truth for Spec 003. A checked item requires repository evidence on the accepted branch/head; nearby work does not imply completion. SQL Studio and LLM Observatory are explicit follow-on specifications and are not silently included by completing this checklist.

## Phase 1 - Canonical Baseline and Dependency Decision

- [x] **T042** Establish Spec 003 with `spec.md`, `plan.md`, `tasks.md`, and `research.md` on a dedicated branch rooted at canonical main `8e92c5612a9ddc32996ed5e08475e3c9baa5e161`. The specification explicitly authorizes the 0.2 workspace/PTY/native-Windows expansion while retaining the no-daemon/no-public-protocol/no-plugin-system boundary.
- [x] **T043** Complete the PTY dependency/provenance decision. `portable-pty` crate `0.9.0`, from `wezterm/wezterm` published-source commit `f8921727a11b9f8b073e8c24821d72fd41283500` path `pty/`, MIT, is accepted as the preferred first direct-dependency candidate. The audit covers required PTY/ConPTY API shape, Linux/macOS/Windows implementation direction, Rust 1.97.1 compatibility posture, direct/transitive-footprint risks, license/notice obligations, mandatory `serial2` footprint, and alternatives (`xpty`, `rust-pty`, `portable-pty-psmux`, Unix-only PTY crates). Evidence is recorded in `pty-dependency-decision.md` and `docs/provenance/donors.md`. No donor runtime code was copied and the crate is intentionally **not yet landed**: the first runtime PR that actually uses it must request exact `portable-pty = "=0.9.0"`, commit the Winds-resolved `Cargo.lock`, compile/clippy/test that exact graph under pinned Rust 1.97.1, and re-audit the exact locked transitive/license set. If those landing gates fail, the candidate decision must be reopened rather than silently worked around.

## Phase 2 - Workspace and Execution Persistence

- [x] **T044** Add a forward-only SQLite migration and store/domain types for a minimal workspace registry plus common execution ledger and typed terminal-session records. Keep existing candidate/evidence/promotion table semantics unchanged; do not add speculative SQL/LLM columns or a generic plugin payload. **Canonical evidence:** PR #10 merged as `870431c63790878f5fc7ddd60a14b14544557512`; exact implementation head `413b9a05ad1f98f8199012f784bf79690a093d58` passed quality #138, release-candidate #24, Ubuntu/macOS format+Clippy+tests, SC-001 100-cycle soak, Linux/macOS release builds, correctness/safety and Ponytail review, Qodo exact-head re-review with 0 bugs / 0 rule violations, and zero unresolved review threads. Post-merge quality #139 passed on exact canonical main `870431c63790878f5fc7ddd60a14b14544557512`. The accepted slice adds only `workspaces`, `executions`, `execution_events`, and `terminal_sessions` persistence plus typed Store/domain records and deterministic tests; it does not land `portable-pty`, PTY/terminal runtime, CLI, SQL/LLM-specific schema, Agent Fleet runtime, or changes to existing verify/promote/recover semantics.
- [x] **T045** Implement existing-worktree open/inspect: canonical worktree root, Git common directory, exact HEAD, branch/detached state, deterministic dirty-state observation, and safe workspace registration outside the repository boundary. Reject non-Git, bare, missing, or ambiguous workspace identity. **Canonical evidence:** PR #12 exact implementation head `a6023fe9cc79521af8f3568a1ec50cc62d9cddad` passed quality #150, release-candidate #32, Ubuntu/macOS format+Clippy+tests, SC-001 100-cycle soak, Linux/macOS release builds, exact-head correctness/safety and Ponytail review, Qodo exact-head review with 0 bugs / 0 rule violations after resolving registration-race and error-string findings, and zero unresolved review threads. PR #12 merged as `f0c98fa1c6cdfd36161463c30ddf02eb08f7b16d`; post-merge quality #151 passed on exact canonical main. The accepted slice canonicalizes nested/symlinked worktree identity, records HEAD only when present (including truthful unborn-branch handling), distinguishes attached/detached state, observes dirty state with read-only system Git, validates the actual state root before opening the Store, refreshes `last_opened_unix_ms`, and rejects missing/file/non-Git/bare/broken identities fail-closed. It does not start T046 clone behavior, environment/shell discovery, `portable-pty`, terminal runtime, CLI, SQL/LLM schema, Agent Fleet runtime, or alter verify/promote/recover semantics.
- [x] **T046** Implement explicit system-Git clone + registration with destination validation, failure-before-registration behavior, and credential-safe persisted remote identity. Do not auto-run repository environment/bootstrap configuration after clone and do not claim hostile-clone sandboxing. **Canonical evidence:** PR #14 exact implementation head `086b39216b75bf8b944f7fd36a1523e17b6b47f3` passed quality #161, release-candidate #40, Ubuntu/macOS format+Clippy+tests, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release builds/artifact uploads, internal correctness/safety and Ponytail review, Qodo exact-head review with 0 bugs / 0 rule violations after reconciling all five prior findings, and zero unresolved review threads. PR #14 merged as canonical main `4d66fbc276d3275ba9a1e49d1f21d1e822a39275`; post-merge quality #162 passed on that exact main. The accepted slice reserves and validates an explicit destination before invoking hardened system Git, fails clone before registration, disables Git/OpenSSH interactive prompting for unattended clone, avoids unbounded clone-output buffering, strips credential-bearing URL/SCP user-info and query/fragment material before persistence, applies migration 0003 through the canonical Store migration sequence, and persists workspace identity plus clone origin atomically after mutation-free worktree inspection. A forced SQLite failure fixture proves workspace/origin rows roll back together while the successfully cloned checkout is left truthful rather than automatically deleted. Tracked `.envrc` and `.mise.toml` fixtures are cloned but not executed. The slice does not claim hostile-checkout sandboxing, does not implement a credential manager, does not start T047 environment/shell discovery, does not land `portable-pty`/terminal runtime/CLI, and does not alter verify/promote/recover semantics.

## Phase 3 - Environment and Shell Profiles

- [x] **T047** Implement non-executing workspace environment inventory: OS/architecture, exact Git paths, discovered shell candidates, and presence of selected tool/environment manifests (`.mise.toml`, `.tool-versions`, `.python-version`, `.nvmrc`, `.envrc`, `rust-toolchain.toml`, devcontainer config). Prove no manifest/bootstrap execution and no full environment-value persistence. **Canonical evidence:** PR #16 exact implementation head `b2016882040bbbd5b45e6b1326c6c8766bc2c705` passed quality #172, release-candidate #48, Ubuntu/macOS format+Clippy+tests, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release builds/artifact uploads, internal correctness/safety and Ponytail review, Qodo exact-head review with 0 bugs / 0 rule violations after reconciling the valid manifest-containment and invalid-leaf-type findings, and zero unresolved review threads. PR #16 merged as canonical main `8da211fd7accc9069f6591647ec0bf5e638d94fe`; post-merge quality #173 passed on that exact main. The accepted slice returns runtime OS/architecture, the T045 canonical worktree/Git-common-dir paths, deterministic candidate-only shell paths from `SHELL`/`COMSPEC` plus declarative `/etc/shells`, and a fixed allowlist of manifest-path presence without reading manifest contents or persisting a full environment snapshot. Manifest metadata is fail-closed: only true `NotFound` means absence, intermediate nested-manifest parents may not be symlinks or non-directories, and existing manifest leaves must be regular files or symlinks. Regression fixtures prove `.envrc`/mise/devcontainer contents are not executed or serialized, nested `.devcontainer` symlink traversal is rejected, and invalid manifest leaf types do not silently look absent. The slice adds no dependency, subprocess, SQLite persistence, shell-profile arguments/domain semantics, WSL discovery, PTY/runtime, CLI, SQL/LLM schema, Agent Fleet behavior, or verify/promote/recover semantic change.
- [x] **T048** Implement concrete shell-profile discovery for Linux/macOS and native Windows with exact executable/argument/domain identity. Display names are UX only; launch identity must be explicit and validated at use time. **Canonical evidence:** PR #18 exact implementation head `026f1f77e81e897eec4f6909019f9f29d9685905` passed quality #183, release-candidate #56, Ubuntu/macOS format+Clippy+tests, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release builds/artifact uploads, exact-head correctness/safety and Ponytail review, Qodo exact-head review with 0 bugs / 0 rule violations after resolving the valid unused-`PathBuf` and non-UTF-8 temp-path reliability findings and dismissing the reconciled `profile_id` scope finding, and zero unresolved review threads. PR #18 merged as canonical main `786b618994ab87e7ef54b4cff2e6c255d32dea57`; post-merge quality #184 passed on that exact main. The accepted slice adds typed native-host shell profiles with deterministic `profile_id` derived only from launch-significant domain/executable/arguments/cwd data, UX-only display names excluded from identity, exact absolute executable paths, an intentionally empty argument vector, `WORKSPACE_ROOT` cwd strategy, filesystem-only candidate usability checks, and launch-time revalidation without executing the shell. Native-Windows candidate construction is source-gated to T047 `COMSPEC`, PATH `pwsh.exe` / `powershell.exe` / `cmd.exe`, and the standard Windows PowerShell / PowerShell 7 locations; every candidate still requires exact file usability. The slice does not claim repository-wide native-Windows compile/runtime proof before T061 and adds no WSL/T049 behavior, dependency, subprocess, PTY/runtime, SQLite persistence, CLI, SQL/LLM schema, Agent Fleet behavior, or verify/promote/recover semantic change.
- [x] **T049** Implement WSL distribution discovery on Windows through supported `wsl.exe` machine-usable behavior. Record distribution identity/version/state when available and fail explicitly when WSL discovery is unavailable or ambiguous. **Canonical evidence:** PR #20 exact implementation head `4cb6e05565c812b7a62d5d162c82cf745d3e62b7` passed quality #191, release-candidate #60, Ubuntu/macOS format+Clippy+tests, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release builds/artifact uploads, exact-head correctness/safety and Ponytail review, CodeRabbit exact-head re-review with no actionable comments and minimal merge risk, and zero unresolved review threads after reconciling Qodo's valid header-collision, environment-derived executable-path, and unbounded-output findings plus the later CI/Clippy findings. PR #20 merged as canonical main `8cc07649f65a7fd3acbfaf6794ce9c63e240f34b`; post-merge quality #192 passed on that exact main. The accepted slice resolves the Windows system directory through `GetSystemDirectoryW`, validates the exact system `wsl.exe`, uses `--list --quiet` as the distribution-identity source and `--list --verbose` only to attach state/version, reconciles both queries fail-closed, preserves localized/multi-word state text, rejects duplicate/missing/drift/unsupported-version cases, supports UTF-8/UTF-16LE output, and bounds concurrently drained stdout/stderr to 1 MiB per stream. It does not launch a WSL distribution, map Windows/WSL paths, add WSL shell profiles, land `portable-pty`, start T050+ terminal runtime, persist new SQLite records, add CLI/Agent Fleet/protocol behavior, or claim repository-wide native-Windows or real Windows+WSL2 support before T061/T062 evidence.

## Phase 4 - PTY Terminal Lifecycle

- [x] **T050** Implement the smallest PTY-backed interactive terminal controller on Linux/macOS using the accepted T043 dependency/primitive. Support exact session identity, start cwd, input/output streaming, resize, interrupt, observed exit, terminate/close, and reaping only for resources/processes Winds can prove it owns. **Canonical evidence:** PR #23 exact implementation head `f9346f95f3949432538e8cbe9e7c9b7acd1dcf6c` passed quality #209, release-candidate #75, Ubuntu/macOS format+Clippy+tests, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release builds/license bundles/artifacts, final exact-head correctness/safety and Ponytail review, Qodo exact-head review recommending the implementation as the best fit for T050, CodeRabbit exact-head review with no blocking correctness/safety issue, and zero unresolved review threads. `portable-pty` is exact-pinned to `0.9.0`; Cargo generated the committed lockfile under Rust 1.97.1; the exact transitive/license audit is recorded in `docs/provenance/portable-pty-0.9.0-lock-audit.md`; and the fail-closed release collector was extended only for the two exact `winapi-*-pc-windows-gnu 0.4.0` package tuples using byte-for-byte upstream MIT/Apache texts from `retep998/winapi-rs` tag `0.3.9`. PR #23 merged as canonical main `44999cc3e383ceb28d23028428d0c9f2b7e997f7`; post-merge quality #210 passed on that exact main. The accepted Unix-only slice revalidates the exact T048 profile before launch, requires a canonical absolute directory cwd, creates a real PTY, exposes one output consumer, supports input/resize/current-size, validates live PTY-session ownership before foreground-group SIGINT, observes/caches final child exit, and terminates/reaps only through the retained child handle. It does not claim durable session identity, restart attachment, descendant-tree cleanup, T051 native-Windows/ConPTY support, T052 WSL launch/mapping, T053 persistence, T054 telemetry, T057 CLI, terminal rendering, daemon/public protocol, MCP/ACP/plugin, or Agent Fleet behavior.
- [x] **T051** Extend the terminal controller to native Windows using the accepted pseudoconsole/ConPTY path without weakening existing Unix process/check safety. Add compile/test gates on an official Windows runner before claiming native Windows workspace/terminal support. **Canonical evidence:** PR #25 exact implementation head `9b8d416886727b046d6a589f705f7a3c41fa29f0` passed quality #239, windows-terminal #27 on official `windows-latest`, release-candidate #102, Ubuntu/macOS format+Clippy+tests, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release bundles, final exact-head correctness/safety and Ponytail review, Qodo final exact-head review with no new actionable correctness/safety/rule issues, and zero unresolved review threads after repairing CodeRabbit's valid unowned-descendant test finding. PR #25 was squash-merged with expected-head guard as canonical main `6b9e5b87e645f029a2ba14460eb946e3b33ec7d4`; post-merge quality #240 and windows-terminal #28 passed on that exact canonical main. The accepted slice reuses exact-pinned `portable-pty 0.9.0`/ConPTY for native-Windows PTY start, canonical cwd, single output-reader ownership, input/output, resize/current-size, exit observation, and directly-owned-child terminate/close/reap; keeps Unix T050 foreground-process-group SIGINT behavior unchanged; rejects unsupported native-Windows required-check execution before verify/promote mutation; and adds a bounded focused Windows CI gate. Official-Windows experiments could not prove a safe ownership-scoped ConPTY foreground interrupt, so FR-012/SC-003 and `research.md` were reconciled to require explicit fail-closed native-Windows `interrupt()` without false success or process-global signaling while proving the live session remains usable. The terminate fixture uses blocking `cmd.exe` built-in `set /p` rather than an unowned long-lived descendant. This slice does not implement WSL launch/path mapping (T052), persistence/restart ownership (T053), telemetry (T054+), CLI expansion (T057), broad touched-surface Windows CI (T061), real Windows+WSL2 integration (T062), daemon/public protocol/plugin/MCP/ACP, or Agent Fleet behavior.
- [x] **T052** Implement explicit WSL terminal launch by selected distribution/profile. Validate effective cwd and repository identity after launch when a workspace path is mapped; expose/fail safe on mapping mismatch rather than silently claiming equivalence. **Canonical evidence:** PR #27 exact implementation head `653a2ea40d200725006c1b468bc6bc8fcdd9f825` passed quality #275, windows-terminal #61 on official `windows-latest`, release-candidate #135, Ubuntu/macOS format+Clippy+tests, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release bundles/artifacts, final exact-head correctness/safety and Ponytail review, Qodo final exact-head delta review with no new actionable correctness/safety/active-spec issue, and zero unresolved review threads after repairing the valid launch-profile integrity, WSL helper reader-lifetime, truncation-diagnostic, pre-launch mapping-attestation, and owned-helper cleanup findings; the generic unused-abstraction rule was reconciled as non-actionable because T052 owns the backend launch surface while T057 owns the later CLI caller. PR #27 merged with expected-head and unchanged-main guards as canonical main `5cbe090a0c704aea0fdbed3c36a04c9b0e37e794`; post-merge quality #276 passed on that exact canonical main. The accepted slice binds WSL launch to an explicitly selected distribution, exact system `wsl.exe`, fixed exact `/bin/sh`, explicit empty shell arguments, execution-domain identity, and mapped-workspace-or-home-fallback cwd strategy; maps the Windows canonical worktree through supported `wslpath` but treats path conversion as insufficient until effective WSL cwd, WSL Git worktree root, Git common directory, and exact `HEAD` are attested against canonical Windows Git identity immediately before ConPTY spawn and again after launch. WSL helper commands strip inherited Git-context variables, bound captured output, preserve bounded diagnostics, and attempt truthful cleanup of the directly owned launcher on every post-spawn helper failure. If mapping equivalence cannot be proven, the launch plan records an explicit fallback to the selected distro home and revalidates it at use time. The slice reuses the T051 terminal controller through a private exact-launch seam and does not persist terminal lifecycle transitions (T053), add command telemetry/history (T054-T056), add CLI proof surfaces (T057), claim broad native-Windows touched-surface coverage (T061), or claim real Windows+WSL2 support before T062 evidence; it adds no daemon/public IPC/plugin/MCP/ACP/Agent Fleet behavior.
- [x] **T053** Persist terminal lifecycle transitions into the execution ledger, including start/final state/timing/domain/profile/cwd and explicit failed/interrupted states. After restart, any session whose continuing process ownership cannot be proven MUST reconcile to `OWNERSHIP_LOST` with process state unknown. A persisted PID alone is not process identity; recovery MUST NOT blindly signal/kill it. No daemon or cross-restart process attachment is allowed in this slice. **Canonical evidence:** PR #29 final exact implementation head `e2121b70b37839a95be8352c55ac63e86ae5052a` passed quality #310, windows-terminal #94 on official `windows-latest`, release-candidate #167, Ubuntu/macOS format+Clippy+tests, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release builds/artifacts, final correctness/safety and Ponytail review, Qodo final exact-head review with no actionable correctness/safety/active-spec findings, and zero unresolved review threads. The final source tree also passed five repeated default-parallel full macOS test cycles after reconciling PTY lifecycle test races. PR #29 merged with expected-head and unchanged-main guards as canonical main `aebf35057f5d5c5133961b008d0cb7c12b8d1ea7`; post-merge quality #311 passed on that exact canonical main. The accepted slice reuses the existing execution/terminal-session schema without migration, atomically records request/session identity and guarded lifecycle transitions, persists typed close reasons and timing only when proven, keeps Drop cleanup ledger-aware and bounded through directly owned child handles, retains Drop-time SQLite finalization failures in a Store-owned in-process retry queue, and reconciles unowned `REQUESTED`/`RUNNING` terminal executions after restart to conservative `OWNERSHIP_LOST` with unknown end/duration. It persists no PID and performs no PID lookup, blind signaling, cross-restart process attachment, daemon behavior, command telemetry/history (T054-T056), CLI proof surface (T057), broad Windows claim (T061), real Windows+WSL2 claim (T062), public protocol/plugin/MCP/ACP, or Agent Fleet behavior.

## Phase 5 - Command Observability and Privacy

- [x] **T054** Add command-level records only for shells with a reliable ephemeral lifecycle integration or an explicit Winds-run command. Capture command/cwd/exit/duration where available without scraping arbitrary PTY keystrokes or editing persistent shell profiles. Hook-emitted command telemetry MUST remain explicitly shell-reported rather than `WINDS_OBSERVED` unless an independently protected Winds observation proves the fact; test marker spoof/confusion by child output. **Canonical evidence:** PR #31 final exact acceptance head `f400f01167c5cf90cbe3235e7477fb98b9213818` passed quality #318, windows-terminal #100 on official `windows-latest`, release-candidate #172, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release builds/artifacts, final correctness/safety and Ponytail review, Qodo fresh exact-head delta review with no actionable correctness/safety/active-spec issues, and zero unresolved review threads. PR #31 merged with expected-head and unchanged-main guards as canonical main `5d2de82c32137b8aea372412c103a69c158a6db7`; post-merge quality #319 and windows-terminal #101 passed on that exact canonical main. The accepted slice adds an explicit structured Winds-run command path with direct absolute executable plus argv semantics, caller-requested executable/arguments/cwd provenance, Winds-observed lifecycle and durable exit facts, conservative failed/interrupted/ownership-lost reconciliation, restart finalize-before-ownership-loss behavior, and non-regressing optional wall-clock evidence so unavailable/regressed timing cannot fabricate timestamps or strand a proven exit as falsely live. It intentionally adds no shell hook or marker parser, no arbitrary PTY keystroke inference, no persistent shell-profile edits, no stdout/stderr retention or broad history/secret policy before T056, no CLI/timeline surface before T057, no broad Windows or real Windows+WSL2 claim before T061/T062, and no daemon/public IPC/plugin/MCP/ACP/Agent Fleet behavior.
- [x] **T055** Add before/after lightweight Git observations around supported command boundaries where reliable, using system Git machine-readable state rather than recursive repository hashing. Interactive mutations remain workspace history and MUST NOT become verification eligibility evidence. **Canonical evidence:** PR #33 final exact implementation head `59d1abe0d22e6b7f11dbfa21a4b8edde2f3df0ee` passed quality #328, windows-terminal #107 on official `windows-latest`, release-candidate #178, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release builds/license bundles/artifacts, exact-head correctness/safety and Ponytail v4.9.0 review, Qodo fresh exact-head T055 review with no actionable correctness/safety/active-spec issues, and zero unresolved review threads. PR #33 merged with expected-head and unchanged-main guards as canonical main `c7bf630063e72e89a1bff1bb7178b0cdf43e207d`; post-merge quality #329 and windows-terminal #108 passed on that exact canonical main. The accepted slice adds a forward-only typed execution Git-observation table and records BEFORE/AFTER system-Git facts around the explicit Winds-run command seam, revalidating registered canonical worktree/common-dir identity and reading HEAD/branch/detached/worktree state from one bounded `git status --porcelain=v2 --branch --no-ahead-behind -z --untracked-files=all --ignore-submodules=none --no-renames` snapshot per boundary. Worktree state is a versioned SHA-256 over exact non-header status records rather than recursive repository contents; unavailable/malformed Git state is persisted as explicit `UNAVAILABLE` without synthesized facts; FAILED_TO_START records BEFORE only; AFTER follows durable EXITED lifecycle finalization; and the execution ledger remains strictly separate from candidate/evidence/eligibility/promotion authority. It intentionally adds no T056 history/transcript retention or secret policy, no T057 CLI/timeline surface, no shell hook/PTY keystroke inference/persistent profile edits, no checkpoint commit/ref, no daemon/public protocol/plugin/MCP/ACP/A2A/Agent Fleet behavior, and no broader platform claim.
- [x] **T056** Implement local history/transcript retention and secret-safety policy before broad automatic persistence: bounded storage/quota, truncation/retention metadata, sanitized clone URLs/launch metadata, no full environment values, and a clear way to disable command/transcript history for a session. Do not claim perfect secret detection. **Canonical evidence:** PR #35 final exact implementation head `35141774825701ebe744ce25adf1dab03d17613e` passed quality #344, windows-terminal #121 on official `windows-latest`, release-candidate #191, Ubuntu/macOS format+Clippy+full tests, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release bundles/artifacts, final exact-head correctness/safety/privacy and Ponytail v4.9.0 review, and zero unresolved review threads. CodeRabbit's final incremental exact-head review completed without a new unresolved finding; Qodo's final Deep review reported Bugs (0), with the remaining rule-level objection to the numeric 8 MiB ceiling reconciled as a non-public Winds allocation-safety bound rather than a Spec requirement. PR #35 was squash-merged with an exact expected-head guard as canonical main `923ac872a14d75d5362695c4d7b385a0227b2f24`; post-merge quality #345 and windows-terminal #122 passed on that exact canonical main. The accepted slice keeps transcript persistence local, bounded, explicit opt-in and default-off; supports per-session command/transcript disable; accounts transcript plus manifest bytes under total quota with explicit truncation/completion metadata; conservatively sanitizes secret-bearing arguments and URL-like launch metadata without claiming perfect detection; persists no full environment snapshot; requires matching typed terminal identity; performs recursive cleanup only after strict owned-descendant validation; and releases the primary `winds.db` writer transaction before history prune/write/fsync, using a coordination-only local SQLite lock carrier for cross-process filesystem serialization. History artifacts remain workspace/session history only and do not become `BlobEvidence`, eligibility, promotion input, or `winds verify` authority. This slice adds no T057 CLI/timeline surface, daemon/server/socket, public IPC/runtime protocol, plugin/provider framework, MCP/ACP/A2A, Agent Fleet behavior, dependency, schema migration, or verification-authority change.

## Phase 6 - Minimal User/CLI Proof Surface

- [x] **T057** Add the minimum CLI surface needed to prove the backend without a GUI: open/inspect workspace, clone/register, list shell/execution profiles, launch a selected terminal/profile or focused interactive proof command, and inspect execution/session metadata in deterministic JSON. Keep command structure small and consistent with existing `winds` CLI behavior. **Canonical evidence:** PR #37 final exact implementation head `680d8e8894a0c6bfd1ef62a5bd373ef1f0a48d0e` passed quality #358, windows-terminal #133 on official `windows-latest`, release-candidate #202, Ubuntu/macOS format+Clippy+full tests including binary-facing T057 integration, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release bundles/artifacts, final exact-head correctness/safety/authority and Ponytail v4.9.0 review, Qodo exact-head review with Bugs (0) / Rule violations (0), and zero unresolved review threads after repairing the non-UTF8 fixture, clone-source state-root, and panic-cleanup findings and reconciling the P3 duplicate-home-resolution suggestion as non-actionable under Ponytail. PR #37 was squash-merged with expected-head and unchanged-main guards as canonical main `5eeb311664ffea4dd4aa49137704570ab8f40445`; post-merge quality #359 and windows-terminal #134 passed on that exact canonical main. The accepted slice adds only the flat proof commands `workspace-open`, `workspace-clone`, `profiles`, `run`, `terminal-proof`, and `execution`; reuses existing backend identity/clone/profile/command/PTY/ledger logic; fails clone state-root overlap/local-source placement before mutation where provable; keeps transcript persistence default-off; binds execution inspection to the requested canonical repo; and emits deterministic JSON without introducing daemon/server/socket/public protocol, terminal renderer, persistent detached terminals, remote runtime, plugin/provider/MCP/ACP/A2A/Agent Fleet behavior, new dependency, schema migration, or verification-authority change.
- [x] **T058** Document the explicit workspace-terminal trust boundary: terminal commands run with the launching user's permissions and may mutate the primary checkout/network/filesystem/secrets; `winds verify` remains the isolated authoritative verification path. **Canonical evidence:** PR #39 final exact documentation head `88987d1aa87d8bed15cb383f4829e960d4045ebc` passed quality #363 on Ubuntu/macOS after an infrastructure-only GitHub codeload HTTP 429 on the initial macOS setup was rerun on the same exact head, exact-head correctness/safety/authority and Ponytail v4.9.0 review, independent exact-head Cubic review with **No issues found across 1 file**, and zero unresolved review threads. PR #39 was squash-merged with expected-head and unchanged-main guards as canonical main `096d80ee3f6831665289c57d1d099aac01580005`; post-merge quality #364 passed on that exact main after the same infrastructure-only macOS codeload 429 was rerun successfully without changing the commit. The accepted slice adds only `terminal-trust-boundary.md`: it states that workspace terminals and explicit `winds run` execute with the launching user's permissions and may mutate/access the primary checkout, filesystem, network, and secrets available to that user; PTY/ConPTY ownership is lifecycle ownership rather than OS/network/filesystem/credential confinement; persisted workspace history and execution-ledger facts do not become candidate eligibility, promotion authority, or verification evidence; and `winds verify` isolation means candidate-worktree/evidence separation rather than an OS sandbox. It adds no T059+ behavior, source/dependency/migration/workflow/runtime change, daemon or persistent detached sessions, public protocol, remote runtime, plugin/provider system, MCP/ACP/A2A, Agent Fleet, Herdr/Pi transplant, broad sandbox framework, or verification-authority change.

## Phase 7 - Deterministic, Fault, Platform, and Soak Evidence

- [x] **T059** Add deterministic negative fixtures for invalid/bare/symlinked workspaces, credential-bearing clone URLs, clone failure, disappearing shell executable, PTY allocation/start failure, immediate shell exit, huge output bound, and environment manifests that must not auto-execute. **Canonical evidence:** PR #41 final exact implementation head `d39549d3190a4e069d3313df2563f86ba999b78b` passed quality #370, windows-terminal #138 on official `windows-latest`, release-candidate #206, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release builds/artifacts, exact-head correctness/safety/authority and Ponytail v4.9.0 review, independent CodeRabbit exact-head review with `Review completed` / success, and zero unresolved review threads. The first Windows attempt failed before checkout/project execution because GitHub codeload returned HTTP 503/429 while downloading the pinned Rust action; only that failed job was rerun successfully on the unchanged exact head. PR #41 was squash-merged with expected-head and unchanged-main guards as canonical main `64b1a2e67a18a53ed17091ea7754e9d4b1fac077`; post-merge quality #371 and windows-terminal #139 passed on that exact canonical main. The accepted slice is tests-only: one dedicated T059 acceptance module plus `#[cfg(test)]` wiring. It proves fail-closed invalid/bare/symlinked workspace handling, local credential-bearing clone sanitization in returned and persisted identity, clone failure before registration, stale shell executable rejection, unavailable-interpreter PTY start/exec failure as a truthful stable non-zero final exit without script-body execution, immediate-exit finality, 1 MiB observed output with 4 KiB retained transcript under a 64 KiB total logical quota, and non-executing/non-secret-serializing environment-manifest inventory. It adds no T060+ fault-injection/race behavior, source/dependency/migration/workflow/runtime change, daemon/public protocol/plugin/provider/MCP/ACP/A2A/Agent Fleet/Herdr/Pi behavior, or verification-authority change.
- [x] **T060** Add lifecycle race/fault fixtures covering input/resize concurrent with exit, interrupt/close escalation while ownership is proven, output-reader failure, SQLite failure before/after child spawn, partial execution persistence, stale ACTIVE session reconciliation to `OWNERSHIP_LOST`, explicit PID-reuse/no-blind-signal behavior, and shell-hook marker spoofing. No false-success, falsely-live, falsely-owned, or falsely-`WINDS_OBSERVED` record is acceptable. **Canonical evidence:** PR #43 final exact implementation head `5b7c1f98fc83eb075fc19a71fbf32ce67e5d9e56` passed quality #392, windows-terminal #158 on official `windows-latest`, release-candidate #225, Ubuntu/macOS format+Clippy+full 118-test suites, SC-001 100-cycle soak, Linux x86-64 and macOS arm64 release bundles/artifacts, final exact-head correctness/safety/authority and Ponytail v4.9.0 review, Qodo exact-head review with Bugs (0) / Rule violations (0), and zero unresolved review threads; CodeRabbit's final rerun was rate-limited and is not counted as a passed final review. The fault suite exposed and repaired two real lifecycle gaps: restart reconciliation now uses a `LEFT JOIN` so partially persisted non-final TERMINAL executions without typed session rows reconcile fail-closed to `OWNERSHIP_LOST` without fabricating session/PID/process truth, and explicit terminal `close()` now reuses the existing 500 ms bounded owned-child cleanup path so unproven reaping returns an error and falls through to durable `OWNERSHIP_LOST` truth rather than blocking indefinitely or claiming false interruption/exit. The accepted fixtures cover live input/resize around owned termination with stable post-final truth, canonical trapped-child interrupt followed by bounded close with only proven-close or ownership-lost outcomes, reader failure with incomplete/truncated transcript truth, SQLite failures before spawn and at RUNNING/EXITED persistence boundaries, deferred finalization retry, partial REQUESTED/RUNNING persistence, stale PID-shaped metadata without blind signaling, and marker-spoof rejection. PR #43 was squash-merged with expected-head and unchanged-main guards as canonical main `16bb2711d745f8ffdb6e01307a2e6e7d601dc89b`; post-merge quality #393 and windows-terminal #159 passed on that exact canonical main. It adds no dependency/schema/migration/workflow/CLI/daemon/public protocol/plugin/provider/MCP/ACP/A2A/Agent Fleet behavior, no cross-restart attachment, and no verification-authority change.
- [x] **T061** Add Linux/macOS terminal integration coverage and a native Windows CI job proving the Spec 003 touched surface. Do not weaken or skip existing 0.1 quality/safety checks solely to make Windows compile. **Canonical evidence:** PR #45 final exact implementation head `4aa905b5162aeaba710c5327a30d5a97f6448b48` passed quality #407, windows-terminal #171, and release-candidate #236, including Ubuntu/macOS terminal integration, the full native-Windows Spec 003 touched-surface suite, SC-001 100-cycle soak, and Linux/macOS release bundles; PR #45 merged as `75975c81aa8b6932c77c026218a53764969ba3d9`. Two late post-merge Qodo findings were then reconciled rather than ignored: the lowercase/mixed-case verbatim-UNC namespace claim was classified unsupported for the documented Winds/Rust path surface with no production widening, while the valid missing canonical-identity mismatch coverage was restored. Follow-up PR #46 exact accepted head `551b9412429196ef8eb20e4e8fcd17b526062c9e` / tree `fa71928d94bec5b5a1ba1133ffa967df5dfe22e3` passed quality #412, windows-terminal #176, and release-candidate #240, including the full native-Windows touched-surface suite, SC-001 100-cycle soak, and both release bundles; a valid CodeRabbit UNC-backed Windows temp-path test portability finding was repaired on that exact head. Final correctness/safety and Ponytail v4.9.0 reviews passed; fresh Qodo exact-head review found no blocking correctness/safety/scope/test-integrity issue and no findings; fresh CodeRabbit exact-head review produced no actionable comments with minimal merge risk; all review threads were resolved. PR #46 merged as canonical main `ad4625ecd7f9a933613890cca74129857d0b4166`, whose tree is exactly the accepted candidate tree `fa71928d94bec5b5a1ba1133ffa967df5dfe22e3`. This closes only T061 platform/touched-surface evidence. It does not constitute T062 real Windows+WSL2 integration proof and does not authorize daemon/server/socket/plugin/provider/MCP/ACP/A2A/Agent Fleet/Herdr runtime work.
- [x] **T062** Obtain real Windows+WSL2 integration evidence before claiming WSL support: distribution discovery, explicit Ubuntu/selected-distro launch, cwd mapping verification, Git identity verification, and visible mismatch behavior. A mocked command-construction test alone is insufficient for the support claim. **Canonical evidence:** PR #50 final accepted head `c109aa9a8a812295c34f341f7db291d513419b5d` / tree `300af6c6af05ccf007adc27020f3762d657a5eaf` passed quality #438, windows-terminal #195, and release-candidate #260, including Ubuntu/macOS quality and terminal integration, the full native-Windows touched surface, SC-001 100-cycle soak, both release bundles, and a real `windows-2025` + Ubuntu WSL2 job. Artifact `9308372989`, digest `sha256:6da9ff17ce314b9745d23007b3a5a3b9d1958d8e2896e0491b41d7283bdf63a5`, is bound to that exact head and records Ubuntu WSL2, production mapped and fallback `prepare_wsl_terminal_launch` / `launch_wsl_terminal` PASS, shell-observed mapped cwd `/mnt/d/a/Winds/Winds`, exact mapped Git HEAD, deliberate automount-disabled `VISIBLE_CWD_MISMATCH` with observed cwd `/` and diagnostic `chdir(/mnt/d/a/Winds/Winds) failed 2`, plus fallback `/root` / `\\wsl.localhost\Ubuntu\root` proven non-equivalent to the mapped workspace. The initial macOS arm64 release job failed at runner setup before checkout and was rerun successfully on the unchanged exact head; it is treated as infrastructure-only evidence, not a repository repair. Exact-head correctness/safety/evidence-integrity and Ponytail v4.9.0 reviews passed; fresh Qodo exact-head review found no new T062 integrity issue; CodeRabbit's final exact-head request was rate-limited and is not counted; all review threads were resolved. PR #50 squash-merged with expected-head guard as canonical main `c20fd3d1704ff6ed42de3fce077e0973420cb606`, whose tree exactly equals the accepted candidate tree `300af6c6af05ccf007adc27020f3762d657a5eaf`. This closes only T062 real Windows+WSL2 support evidence and does not start T063+, daemon/server/socket/public protocol/plugin/provider/MCP/ACP/A2A/Agent Fleet/Herdr/Pi work.
- [x] **T063** Run a deterministic 100-cycle controlled terminal lifecycle soak: create -> command/input -> resize -> observe exit -> close/reap -> reconcile store. Require zero leaks of directly owned children during controlled lifecycle, zero falsely-live terminal rows, bounded retained output, and no corruption/regression of candidate/evidence/promotion tables. Unexpected-crash recovery is separately proven by `OWNERSHIP_LOST` truth rather than claiming unknown survivors can always be killed. **Canonical evidence:** PR #52 final accepted head `e6b02f9c320055b9ed0f7b5d562afd77ea481ebf` / tree `69273499738428f0de29096eb7eac9a8134a0e61` passed quality #454, windows-terminal #208, and release-candidate #272, including the T063 100-cycle lifecycle soak on Ubuntu 24.04, macOS 15, and Windows Server 2025, the supplemental live-child active-close and Windows child-observed ConPTY resize guard on all three matrix hosts, fail-closed exact-one-test execution, SC-001, and both release bundles. Exact-head correctness/safety/evidence-integrity and Ponytail v4.9.0 reviews passed; fresh Qodo exact-head review found no blocking issues; Cubic's three valid evidence gaps were repaired and all review threads were resolved. PR #52 squash-merged with expected-head guard as canonical main `3cb8d143f119b85b93c4194a8f58c88b19279edc`, whose tree exactly equals the accepted candidate tree `69273499738428f0de29096eb7eac9a8134a0e61`. This closes only T063 and does not start T064+ or authorize daemon/server/socket/public protocol/plugin/provider/MCP/ACP/A2A/Agent Fleet/Herdr/Pi work.
- [ ] **T064** Run the complete pre-existing `winds verify/promote/recover` deterministic suite on the exact implementation head and prove no regression in existing evidence authority, non-destructive candidate worktrees, promotion recheck, or recovery behavior.

## Phase 8 - Acceptance and Review

- [ ] **T065** Update README/CONTRIBUTING/SECURITY/relevant docs for accepted 0.2 workspace-terminal behavior only. Do not describe SQL Studio, LLM Observatory, persistent detached terminals, terminal renderer, or native-Windows verification as implemented unless separately proven.
- [ ] **T066** Complete correctness/safety review on the exact final implementation head, explicitly covering PTY/process ownership, stale PID reuse, Windows/Unix close semantics, WSL path/domain truth, SQLite partial transitions, shell-telemetry source attribution, secret/history persistence, and separation from verification authority.
- [ ] **T067** Complete Ponytail v4.9.0 simplicity review on the exact final implementation head. Challenge every dependency/module/protocol; remove custom multiplexer/renderer/plugin/provider/environment-manager machinery not required by Spec 003.
- [ ] **T068** Obtain and reconcile at least one independent reviewer pass on the exact final implementation head. External summaries or reviews bound only to older heads do not satisfy this task.
- [ ] **T069** Reconcile deterministic CI, platform/WSL evidence, soak results, correctness/safety, Ponytail, and independent-review findings into final canonical task truth before making the Spec 003 completion claim.

## Explicit Follow-On Specifications

These are product goals, **not hidden unchecked work inside Spec 003**:

- **Spec 004 - SQL Studio**: secret-safe connection profiles, schema/catalog intelligence, query editor/history, explicit transaction/write safety, dialect-aware parsing, timing/rows/results/export, EXPLAIN/plan evidence, and workspace-linked query executions.
- **Spec 005 - LLM Observatory**: provider/model traces, input/output/cache/reasoning tokens, total/streaming latency, retries/errors/rate limits, exact or unknown cost with pricing provenance, budgets/aggregates, tool/subagent spans, payload privacy, and OpenTelemetry-aligned export.
- **Later UI slice**: graphical workspace organization, panes/tabs, embedded terminal renderer, SQL data grid/plan visualization, and LLM timeline dashboards built on the proven backend records.
- **Later persistence slice**: detached live terminal sessions across Winds restarts only if real user need justifies an external PTY owner/daemon and a versioned reconnection protocol.